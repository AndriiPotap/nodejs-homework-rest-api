const ctrlWrap = require("./ctrlWrap");
const validation = require("./validation");
const authenticate = require("./authenticate");
const upload = require("./upload");
const avatarNormalaizer = require("./avatarNormalaizer");

module.exports = {
    ctrlWrap,
    validation,
    authenticate,
    upload,
    avatarNormalaizer
};